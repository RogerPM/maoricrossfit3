json.extract! header_class, :id, :date, :start_time, :end_time, :maximum_capacity, :registered, :service_id, :shcedule_id, :teacher, :created_at, :updated_at
json.url header_class_url(header_class, format: :json)
